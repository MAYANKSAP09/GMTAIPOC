applications:
  - name: llm-demo-gen-ai-hub-poc
    memory: 2048M
    instances: 1
    buildpacks:
      - python_buildpack
    command: streamlit run app.py --server.port ${PORT} --server.address 0.0.0.0